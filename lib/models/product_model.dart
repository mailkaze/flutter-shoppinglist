class Product {
  Product({
    required this.marked,
    required this.name,
    required this.price,
    required this.quantity,
    required this.uid,
  });

  bool marked;
  String name;
  double price;
  double quantity;
  String uid;
}
